# RESEARCH.md

Companion to [`ARCHITECTURE.md`](./ARCHITECTURE.md), which holds what we believe now; this holds why, what we refused, and what is still owed.

Not append-only. Rows are rewritten when a position changes and deleted when they stop changing a decision — see "Collapse, don't stack" below. Optimised to be read by a session with no memory of this one, not to be a complete account of how we got here.

Long-form research and working notes stay in `.scratchpad/` — untracked, local, disposable. Only what survives contact with a future session belongs here.

## How to write a row

- **One line per row.** If it needs a paragraph, it does not belong here.
- **A decision row without a rejected alternative is a description, not a decision.** Drop it or find the alternative.
- **Facts carry a recheck trigger**, never "forever". Harness versions, registries and star counts all rot.
- **Evidence must survive.** A commit SHA, a git-tracked path, or a URL. Never a `.scratchpad/` path — that directory is ignored and invisible to everyone else.
- **Collapse, don't stack.** When a decision reverses, rewrite the row in place. Keep every alternative still worth refusing; drop the positions we merely passed through. A reader needs what is true now and what not to re-propose — the route we took to get there is archaeology, and git history already holds it. Changing your mind twice in a day is normal and costs nothing; making the next reader page through both attempts is what costs.
- **Delete a row the moment it stops changing a decision.** A ledger earns its place by being read. Every row that no longer alters what someone would do is drag on the ones that do.
- **Open stride states:** `open`, `landed <sha>`, `partial`, `dropped: <why>`.
- **Check the stride table against your own diff before committing.** Rows describing work in progress go stale between being written and being committed — three did on this file's first day. A ledger that misdescribes the repo argues against its own existence.

## D — Decisions

| id  | date       | decision                                                                                                                                                                                                                                                                                                                                                                       | rejected alternative                                                                                                                                                                                                                                                                                                                               | why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | evidence                                                                                                                                                            |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | 2026-05-15 | Commit SHA is the version. No `version` key in any `plugin.json`.                                                                                                                                                                                                                                                                                                              | Pinned semver (`"version": "0.1.0"`) so users can pin and breaking changes get signalled — re-proposed as a P0 three weeks after removal, never executed.                                                                                                                                                                                          | The one version bump we ever made existed only to force a plugin-cache refresh; four minutes later that was recognised as the wrong lever. The SHA already identifies the build. Consequence: updating a plugin may need a manual cache clear, since no version bump triggers a refresh.                                                                                                                                                                                                                                            | `44ea98a` then `91c33bb` (four minutes apart); `plugins/dossier/CHANGELOG.md:5`                                                                                     |
| D2  | 2026-07-20 | Two sibling plugins composed at phase gates. Wire, don't merge.                                                                                                                                                                                                                                                                                                                | Merge into one plugin, since both are effectively always installed together.                                                                                                                                                                                                                                                                       | whetstone stays independently publishable; composition reuses the existing absent-skip adapter pattern. Reaffirmed twice against the always-used-together argument.                                                                                                                                                                                                                                                                                                                                                                 | `plugins/dossier/ADAPTERS.md` §whetstone                                                                                                                            |
| D3  | 2026-08-02 | Four primary operator verbs (`status`, `new`, `close`, `check`) — the budget counts what an operator must remember. Lifecycle verbs (`build`, `ship`, `converge`, `backprop`, `grill`, `verify`, `roll`, `migrate`) ride a stage of the wave or a composed route and sit outside it; `converge` was classed here 2026-08-02 after landing as what looked like a fifth primary. | Grow nine verbs to fourteen by adding `run`, `pause`, `resume`, `clarify`, `pin` — the direction research actually recommended. For converge: promote it to a fifth primary (expands the memory budget a good verb at a time), or demote it to composed-only (removes the operator's named handle on the never-closed-waves problem F17 measures). | Operator rejected command sprawl outright. Consolidation beat expansion; nine became four, not fourteen.                                                                                                                                                                                                                                                                                                                                                                                                                            | root `README.md` "the four you type" table (the old `plugins/dossier/README.md` §Verbs cite died when that README was gutted)                                       |
| D4  | 2026-07-20 | Cross-plugin scripts resolve by source-relative path or an explicit env var only.                                                                                                                                                                                                                                                                                              | Glob-scan the installed plugin cache directory to auto-discover them.                                                                                                                                                                                                                                                                              | Extraction mtimes are not version signals, stale cache dirs survive reinstalls, and cross-marketplace name collisions can select a wrong-provenance script. Rejected by doubt-pass.                                                                                                                                                                                                                                                                                                                                                 | `plugins/dossier/ADAPTERS.md` §whetstone; `5979436`                                                                                                                 |
| D5  | 2026-06    | Restrict read-only skills with `disallowed-tools`.                                                                                                                                                                                                                                                                                                                             | `allowed-tools` to pre-approve verbs and cut permission prompts.                                                                                                                                                                                                                                                                                   | `allowed-tools` is advisory and does not remove tool access — it would have produced a skill that looked constrained and was not.                                                                                                                                                                                                                                                                                                                                                                                                   | `anthropics/claude-code#37683`; `plugins/dossier/skills/status/SKILL.md:4`                                                                                          |
| D6  | 2026-07-06 | `close` and `migrate` stay model-invocable.                                                                                                                                                                                                                                                                                                                                    | `disable-model-invocation: true` on both — shipped, then reverted a month later.                                                                                                                                                                                                                                                                   | Rationale not recorded at revert time. `unconfirmed` — the what is certain, the why is lost. Do not re-adopt without rediscovering the reason.                                                                                                                                                                                                                                                                                                                                                                                      | `0517e7a` (2026-06-05) → `2c01859` (2026-07-06)                                                                                                                     |
| D7  | 2026-07-20 | The doubt gate auto-fires on design-class builds.                                                                                                                                                                                                                                                                                                                              | A `--doubt` opt-in flag plus a declinable offer.                                                                                                                                                                                                                                                                                                   | Optional flag is not activation: whetstone saw zero invocations in 47 days under opt-in. A gate the operator never opts into enforces nothing.                                                                                                                                                                                                                                                                                                                                                                                      | `b2f1939`                                                                                                                                                           |
| D8  | 2026-07-31 | The debt-marker gate does not live in this plugin. Denying `TODO` / `FIXME` / `XXX` / `HACK` and hardcoded credentials at write time is a coding standard, and it moved to the operator's harness (`hook_config.json` `file_mutation.protected_files`), where it applies in every repo and is comment-anchored.                                                                | Shipping it here on by default in every repo — the original D8, which made `export const Status = { TODO: 1 }` a hard deny in projects that never opted in. Then scoping it to a `live`/`paused` wave, which fixed the reach but kept a personal style opinion inside a plugin and left the standard unenforced between waves.                     | Wrong building, not wrong strength. Every sibling PreToolUse hook detects something intrinsically dossier — a ledger header token, a §V citation, a registered invariant. This one detected `TODO`, which has nothing to do with dossiers; the wave scope existed only to justify its address. The harness already gates comment policy, phase markers and secret shapes the same way, so it had a home. Moving it also let the pattern get stricter: comment-anchored, so the object-key false positive is gone.                   | `ARCHI.md` §4.4; harness `hook_config.json`                                                                                                                         |
| D9  | 2026-07-31 | Priorities ordered: Honesty, then Recoverability, then Leverage.                                                                                                                                                                                                                                                                                                               | Leaving tenets unranked and settling conflicts case by case, as had been done until now.                                                                                                                                                                                                                                                           | Seven of the eight recorded conflicts above resolve cleanly under this ordering; the eighth needs the evidence-strength rule instead. Unranked tenets let the `version` question reopen three weeks after it was settled.                                                                                                                                                                                                                                                                                                           | `ARCHITECTURE.md` §Priorities                                                                                                                                       |
| D11 | 2026-07-31 | The invariant-guard registry stays at `.scratchpad/dossier/.invariant-guards.json`, and the hook keeps its no-dossier-tree early exit.                                                                                                                                                                                                                                         | Relocating it to a tracked `.dossier/invariant-guards.json` so the ratchet survives a fresh clone — implemented, then reverted the same day.                                                                                                                                                                                                       | This suite plus the operator's Claude config is one personal harness; it must leave no artifact in a project that did not ask for one, and `.scratchpad` is gitignored by design rather than by accident. Surviving a clone is not even a win: the guard is minted by `ds:backprop` from a §V row whose bug ledger and root cause live in `.scratchpad` too, so a tracked registry would arrive without its provenance — an unexplained block is worse than absence. It is regenerable; re-hit the bug and backprop mints it again. | `151a5be`                                                                                                                                                           |
| D12 | 2026-08-01 | Code-shape enforcement lives at the `ds:build` gate: the one computable rule as a script route at step 7, the four judgment rules as a `Warn:`-capped axis on `dossier-reviewer`.                                                                                                                                                                                              | A PreToolUse hook denying an over-long line at write time — the shape that catches a violation earliest.                                                                                                                                                                                                                                           | Edit-time denial of a pattern that is not dossier-intrinsic is the address D8 already rejected: it ships one repo's taste to every repo the plugin is installed in, and fires on files the wave never touched. The build gate reads the staged diff, so it scopes to the lines a commit ADDS and leaves a repo's existing violations alone — which is what makes it adoptable in a codebase that already has thousands of them.                                                                                                     | `dee790a` `281b945` `18ed452`; D8; the `--cached` diff scoping in `tiger_check.py`                                                                                  |
| D13 | 2026-08-01 | A wave is governed by a CONTRACT (home per D15): a named consumer, and 2-5 `done-when` criteria that are each a command with an expected result. `ds:converge` runs them and returns DONE or the unmet ones.                                                                                                                                                                   | Keep waves as a §T task list and judge completion by reading it — what we did for four waves. Also considered: more review rounds to raise confidence.                                                                                                                                                                                             | A task list says what to do and never what done means, so the objective defaults to the last instruction. Measured over one wave: three review rounds produced 30 findings while runtime lines changed 253 → 191 → 65 → 25, and round three changed nothing user-visible; roughly 7 of 34 bugs were caused by an earlier fix in the same wave, in chains up to three deep. More rounds could not converge because nothing computed "done". A criterion that is a command can be run; prose cannot.                                  | this session's §B/commit analysis; `mattpocock/skills` Completion Criterion (checkable + exhaustive); `github/spec-kit` constitution checks and `/speckit.converge` |

| D14 | 2026-08-02 | Positive-form rails are held to a per-file occurrence ceiling plus a survivors table in the wave contract naming every prohibition left standing; the `positive-rails ceiling` CI step and `ds:converge` each exit non-zero when the count creeps back or the table stops matching. | Ship a `negation-check` lint on `PATH`, the shape `claim-check` already proved. | A lint decides token shape; F28's mechanism is about activation. A file passes a shape lint by appending a positive clause after every prohibition and changing nothing about how the prohibition reads to a model — right behaviour, wrong stated reason, which is the F25 class built as a tool and shipped to consumers. The count is checkable and the survivors table is exhaustive in Pocock's sense: every remaining occurrence was looked at and argued as either a description of system behaviour or a hard guardrail. | `.dossier/2026-08-02-positive-rails.md` criteria 1-4; F28 |

| D15 | 2026-08-02 | Contract home is opt-in: `.dossier/<date>-<slug>.md` where the repo created that directory; else untracked `<wave-dir>/CONTRACT.md`, its costs stated at write time — including that its criteria run as shell from a gitignored file no diff ever showed a reviewer, a checkpoint only the tracked home provides. Resolution prefers tracked; `ds:new` never creates `.dossier/` itself; `ds:close` records the converge verdict in §Z/§S and archives a tracked contract to `.dossier/_archive/`. | Always tracked — the original D13 shape, which committed harness bookkeeping into every consuming repo (visible in shared-repo PRs, accruing forever since close retired nothing). Always untracked — kills citability everywhere, breaks D14's evidence cite, and fails the priority ordering since the contract IS the recoverability artifact. | Leave-nothing-behind is identity (ARCHITECTURE §What this solves; D11's surviving half), while D13's evidence value is real where the operator owns the repo — so the repo chooses, and the degraded home is priced openly instead of hidden. One resolver serves both homes and the prompt hook imports it, closing the O30-class divergence the two transcriptions had already grown. | `converge.py` `_contract_for`; `close/SKILL.md` §5.5/§7.5; `new/SKILL.md` §2.7 |

| D16 | 2026-08-03 | A contract's criteria are named before any of them runs: a `contract: <path>` header, `(wave-dir, untracked)` when that home won, then one `will run <id>. <command>` line per criterion, flushed. | Gate execution instead — require an explicit path or an env opt-in before the untracked `<wave-dir>/CONTRACT.md` is ever auto-resolved, so `ds:close` cannot run a cloned repo's shell. | Criteria are shell and `ds:close` resolves a contract with no argument, so cloning a repo and closing a wave in it runs somebody else's commands. Gating the untracked home would break the flow D15 priced for a repo that never opted into `.dossier/`: the degraded home is meant to work, not to be a trap door. Naming every command first keeps both — the reader sees the block before the first command starts, and the weaker home labels itself. Ordering in the buffer is not ordering for the reader: unflushed, the block reached a pipe only at exit, after every command had run, which is what every caller here reads. | `converge.py` `_untracked` + the flushed plan block in `main`; `test_every_command_is_named_before_the_first_one_runs`, `test_the_plan_block_reaches_a_pipe_while_the_run_is_still_going` |

| D17 | 2026-08-03 | `.editorconfig` section globs are matched by carrying reachable path positions across tokens (`_glob_matches`), not by compiling the header into a regex. | Keep the regex and bound its input: wrap `re.match` in `except re.error` and cap the wildcard count per section. | The header is repo-controlled text with two failure modes of its own. `[[!]]` spliced to `[^]` and raised, and the traceback exits 1 — the checker's own BLOCK code, so a broken config read as a real offence. Backtracking is the worse half, and a cap cannot fix it: measured, `*a*a*a*b` (four wildcards) takes 41 s against a 1024-char path, and `*a*a*a*a*b` (five) takes 113 ms against a 100-char one, so any cap loose enough for `**/*.{js,ts}` is loose enough to stall. Token matching is linear in the path and cannot raise. | `tiger_check.py` `_tokens`/`_glob_matches`; `test_an_unparseable_section_glob_is_not_read_as_a_block`, `test_a_wildcard_heavy_section_glob_does_not_hang` |
| D18 | 2026-08-16 | A gate whose threshold encodes one operator's taste lives in the rig; a gate that encodes a fact about correctness ships in the plugin. The response-length ceiling is 40 lines in `~/.dotfiles-private`; the plugin ships `claim-check --stdin`, which carries no threshold. | Shipping the length gate as a whetstone hook, which "enforcement must travel with the plugin" would normally favour. | That tenet is about reach, not about content, and following it here would ship a line budget to everyone who installs whetstone. This is D8's principle reached a second time from a different artifact, which is why it earns a row rather than a cross-reference: D8 moved a debt-marker gate out, this one decided where a new gate is born. The split test: does the gate encode a fact about correctness, or a preference about form? An unbacked-claim lint is the first; a reply length is the second. | `~/.dotfiles-private` `ad90586`, `ec92767`; plugins `a6d6f8a` |

## F — Facts with a shelf life

| id  | verified                                         | fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | source                                                                                                                                                                                                           | recheck trigger                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | 2026-07-20, Claude Code 2.1.205                  | Referencing an absent plugin's `subagent_type` raises a recoverable tool error naming the available agents — not a silent no-op, not a fallback agent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | live probe; `anthropics/claude-code#59881`, `#68945`                                                                                                                                                             | changelog entry on `subagent_type` error handling; a different error string on re-probe                                                                                                                                                               |
| F2  | 2026-07-20, Claude Code 2.1.205                  | No `SlashCommand` tool exists. Model-invoked skills route through the `Skill` tool (interceptable via `PreToolUse`); typed `/name` goes through `UserPromptExpansion`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `code.claude.com/docs/en/tools-reference`, `/hooks`, `/skills`; consumed at `plugins/dossier/hooks/hooks.json`                                                                                                   | a `SlashCommand` matcher appearing in the tools reference; either hook event renamed, which would silently stop the gate firing                                                                                                                       |
| F3  | 2026-06                                          | `disallowed-tools` enforces at the tool layer — a denied tool is absent, not merely discouraged. `allowed-tools` is advisory and does not restrict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `anthropics/claude-code#37683`                                                                                                                                                                                   | issue closed or fixed; a release note on frontmatter tool-scoping                                                                                                                                                                                     |
| F24 | 2026-07-31                                       | CI installs a pinned `shellcheck`; `ubuntu-latest` ships an older build that flags `SC2015` on `cmd \|\| true` where 0.11.0 does not. Local passed clean and CI failed on the same tree — `--enable=all` locally still gave zero hits, so the gap is version, not configuration. Pinned via a `SHELLCHECK_VERSION` env var that renovate tracks (`customManagers:githubActionsVersions`, added to `renovate.json` because the shared preset omits it), and the step asserts the running version matches the pin. shellcheck is a static analyser, so same version means same verdict on macOS and Linux alike — the pin, not the OS, is what makes them agree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | reproduced on run `30654254811` vs local 0.11.0; renovate's own preset regex verified to match both annotations                                                                                                  | if CI and local ever disagree again, compare `shellcheck --version` on both before touching any script — never add suppressions until the versions match                                                                                              |
| F23 | 2026-07-31                                       | `claude plugin validate <path>` needs no auth and no network — it ran clean under `env -i HOME=$(mktemp -d)` — so it is usable in CI. It exits 1 on a broken plugin and 0 on ours. **Never add `--strict`**: that promotes the "No version specified" warning to an error, and D1 says the commit SHA is the version, so `--strict` fails this repo permanently by design. It is the only checker that caught the unparseable-frontmatter class (`02eb4bc`); `check-jsonschema` and `lint_skill.py` both passed those files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ran locally against 2.1.220; CLI pinned in `ci.yml`                                                                                                                                                              | re-check on any Claude Code minor bump — if `validate` ever fails on a versionless manifest without `--strict`, D1 and this step conflict and one has to give                                                                                         |
| F4  | 2026-06-03                                       | Command hooks cannot call tools. Any design where a hook hydrates the TaskList directly is impossible.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `code.claude.com/docs/en/hooks-guide`                                                                                                                                                                            | hooks-guide revision adding tool invocation to command hooks                                                                                                                                                                                          |
| F5  | 2026-06-05                                       | `CLAUDE_PLUGIN_ROOT` was unset on SessionStart — an upstream bug, since fixed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `anthropics/claude-code#27145` (dup of `#24529`, closed 2026-04-17)                                                                                                                                              | the in-script fallback at `plugins/dossier/hooks/session-start.sh:13` is still load-bearing; do not strip it as dead code without re-confirming the fix holds                                                                                         |
| F6  | 2026-06-05                                       | SessionStart `sessionTitle` is honored on `startup` and `resume`, ignored on `clear` and `compact`, and must only be emitted when the incoming `session_title` is empty. Multi-hook collision winner is undocumented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `code.claude.com/docs/en/hooks`                                                                                                                                                                                  | hooks doc documenting multi-hook `sessionTitle` precedence                                                                                                                                                                                            |
| F7  | 2026-07-07                                       | `CronCreate` is session-bound and fires only while the REPL is idle, auto-expiring after 7 days — it is not a daemon. `/goal`'s evaluator cannot call tools and judges the transcript only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | in-session verification pass                                                                                                                                                                                     | a release changing Cron persistence or `/goal` evaluator capabilities                                                                                                                                                                                 |
| F9  | 2026-08-05, Claude Code 2.1.222                  | The harness documents 31 hook events; this suite wires 7 (`SessionStart`, `UserPromptSubmit`, `UserPromptExpansion`, `PreToolUse`, `PreCompact`, `Stop`, `SessionEnd`). All seven still exist under those names — no rename. Unused and relevant: `PostToolUse`, `SubagentStop`, `TaskCompleted`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `code.claude.com/docs/en/hooks`                                                                                                                                                                                  | `python3 -c "import json;print(sorted(json.load(open('plugins/dossier/hooks/hooks.json'))['hooks']))"` disagreeing with that list either way — a removal or an addition; any wired event absent from that page; new events relevant to an open stride |
| F10 | 2026-07-31                                       | No published JSON Schema exists for `hooks.json`, SKILL.md frontmatter, or subagent frontmatter — prose field tables only. SchemaStore carries exactly two relevant schemas: plugin manifest and marketplace.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `schemastore.org/api/json/catalog.json`; `code.claude.com/docs/en/skills`                                                                                                                                        | a schema appearing for any of the three, which would turn a hand-maintained field table into a real validation step                                                                                                                                   |
| F11 | 2026-07-31                                       | A global git ignore file counts for `git check-ignore`. This operator's `~/.config/git/ignore` excludes `.scratchpad`, so any gate keyed on diffing a file under it is inert in every repo, not just ones with a local rule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `git check-ignore -v .scratchpad/INDEX.md`                                                                                                                                                                       | the global ignore file changing, or the index becoming tracked                                                                                                                                                                                        |
| F12 | 2026-07-31, corpus 2026-06-18..07-31             | Plugin updates disarm live sessions: 287 `Plugin directory does not exist` hook failures over 143 Edit/Write/Stop calls, 13 sessions, 10 projects, 8 stale cache hashes — every hash a commit of this repo. All four PreToolUse guards go dark simultaneously and nothing surfaces conversationally. Direct consequence of D1: commit-SHA versioning makes update frequency equal commit frequency. **Unfixable by shipping code** (2.1.220 binary analysis): the check lives in the harness's variable-substitution path beside `CLAUDE_PLUGIN_ROOT`/`CLAUDE_PLUGIN_DATA` handling, so the plugin directory is resolved and validated BEFORE the command executes — an inline shell fallback never runs, and a shim would sit in the deleted directory. The only in-plugin route is self-resolving the cache, which D4 rejected. Untested: whether a command naming no plugin variable skips the check.                                                                                                                                                                                                                                                         | grep `Plugin directory does not exist` in `~/.claude/projects/**/*.jsonl` attachment stderr                                                                                                                      | re-measure after any survivability fix; any release changing plugin-cache GC or hook substitution                                                                                                                                                     |
| F13 | 2026-07-31, revised by operator same day         | `.scratchpad/` appearing in every project is BY DESIGN, not a defect — it is gitignored by convention, tracking it is the user's choice, and losing it is an accepted degraded experience. Supersedes the earlier reading of this as an artifact violation. Residual issue only: `roll_lib.roll_dir()` resolves against `Path.cwd()` rather than the payload `cwd`, so running from a subdirectory forks state — confirmed as a second `.scratchpad` inside `fCC/freeCodeCamp/api` when the monorepo root already had one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | operator statement 2026-07-31; on-disk sweep                                                                                                                                                                     | if the cwd-relative resolution changes                                                                                                                                                                                                                |
| F15 | 2026-07-31                                       | The whetstone Skill wrappers are not the route anyone takes: `doubt-pass` 2, `tdd-cycle` 0, `merge-resolve` 0, `flaky-test-audit` 0, `skill-smith` 0 over 44 days, while its subagents fired 22 times and dossier's own skills ran 21/21/20/14/10. `skill-smith` and `flaky-test-audit` have no composed path, so their zeros are unambiguous.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `input.skill` census over `~/.claude/projects/**/*.jsonl`                                                                                                                                                        | after any description or packaging change                                                                                                                                                                                                             |
| F16 | 2026-07-31                                       | Exactly one `.invariant-guards.json` exists across the whole dev tree — `fCC-U/veritas`, a single rule `V42` tied to bug `§B5`. The ratchet is near-unused, and F12's outage swallowed that one guard at least twice in the one repo where it had something to enforce.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `find ~/DEV -name .invariant-guards.json`                                                                                                                                                                        | as adoption grows; this bounds the severity of any invariant-guard defect                                                                                                                                                                             |
| F17 | 2026-07-31                                       | Waves complete and are never closed: 44 `ds:new` opens against 10 `ds:close` completions, 9 live on disk today. `~/.dotfiles/.scratchpad/dossier/2026-07-16-wm-rice-perf` has all 13 `§T` rows `x` and a final log line reading "ledger complete 13/13", still `live` after 15 days. The gap is the close step, not the build step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | header-state and `§T` sweep across `.scratchpad/dossier/` trees                                                                                                                                                  | after any close-nudge lands                                                                                                                                                                                                                           |
| F18 | 2026-07-31, landed `018afa3`                     | Consumers receive `hooks/`, `skills/`, `agents/`, `FORMAT.md`, `ADAPTERS.md`, `CHANGELOG.md` and the per-plugin `README.md` — never `.github/` and never the root docs. The plugin READMEs currently link to `../../README.md`, `../../ARCHITECTURE.md` and `../../RESEARCH.md`, all of which resolve to nothing from the install path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `ls ~/.claude/plugins/cache/raisedadead-plugins/<plugin>/<sha>/`                                                                                                                                                 | after the plugin READMEs are repaired                                                                                                                                                                                                                 |
| F19 | 2026-07-31, landed `728ead2` `e5d964e` `a25e91d` | Six documented claims are false from a consumer's position: the fake-impl backstop never fires when only untracked files exist (`git diff --name-only HEAD` is empty); `verify/SKILL.md` claims it fires on every edit but is dormant without `.scratchpad/dossier`; `ARCHITECTURE.md` says all three subagents refuse writes while `dossier-scout` and `dossier-reviewer` carry unrestricted `Bash`; `--auto` "never pushes" rests solely on the operator's private hook config; `test_adapters_whetstone.sh` greps prose; `DS_HEALTH_CMD`/`DS_LIGHT_LOG`/`DS_RECOVER_DAYS` appear in no hook code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | each verified against source and by direct invocation                                                                                                                                                            | after each claim is either made true or reworded                                                                                                                                                                                                      |
| F20 | 2026-07-31, corrected twice                      | Advisory is the right strength for `§`-cites in source, and a deny is unsafe: applying `marker_guard.MARKER_PATTERNS` to every tracked `.go` file in `fCC/artemis` gives 9 hits across 7 files, of which 2 are `SPEC §`-style spec citations the comment allowlist explicitly protects — the regex cannot separate the two kinds. `git blame` puts every hit before the detecting pattern shipped, so the residue proves nothing about whether the nag works. Both earlier versions of this row miscounted and inferred backwards from the count.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `plugins/dossier/hooks/marker_guard.py` `MARKER_PATTERNS` applied over `git ls-files` in the consuming repo, with `git blame` per hit                                                                            | a cite authored after 2026-07-20 in a repo where the guard was demonstrably live — the only observation that would justify deny                                                                                                                       |
| F22 | 2026-07-31, Claude Code 2.1.220                  | `${CLAUDE_PLUGIN_DATA}` is available to plugin-defined hooks; the harness's own diagnostic states it is denied to skill hooks specifically ("${CLAUDE_PLUGIN_DATA} is plugin-only"). Viable for storage that must outlive a working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | same binary analysis                                                                                                                                                                                             | a release changing hook variable availability                                                                                                                                                                                                         |
| F25 | 2026-08-01, re-audited twice                     | The false-enforcement class recurs, and the residue is now characterised. Shape: **right behaviour, wrong stated reason** — nothing the claim predicts ever fails, so no test catches it and it reads as authoritative. Six instances across one wave, every one reviewer-found and none test-found. `claim-check` closes the mechanical half: a sentence whose subject is a backticked name or a definite machinery noun and whose verb is one of six enforcement verbs must name an exit code, a citation or a label. What stays reviewer work is precisely two shapes neither a lint nor a test reaches: cross-file contradiction (`ADAPTERS.md` once answered the tiger-reach question both ways) and a ledger row asserting a diff it did not contain.                                                                                                                                                                                                                                                                                                                                                                                                      | `18ed452`, `7db6895`, `13b349a`; `marker_guard.py` MARKER_PATTERNS against a payload carrying `// Phase 2` returning exit 0; `git diff HEAD` and `git diff --cached` compared byte-for-byte on a staged-new file | a recurrence in a shape `claim-check` could match — that reopens the lint scope. A recurrence in the two named residue shapes is expected and stays with the reviewer.                                                                                |
| F26 | 2026-08-01, Claude Code 2.1.220, n=3             | A backgrounded subagent can go silent: the notification arrives as a bare `idle_notification` with `idleReason: "available"`, no `summary=` and no body, while siblings of the same type spawned in the same call return full reports. Three silent lenses span three agent types, so type does not predict it and the cause stays unverified. Cost is the part that matters: a silent lens is indistinguishable from a lens that found nothing, so a round reporting clean must separate "audited, clean" from "never audited". Track dispatched-versus-delivered, and re-prompt a silent agent **by name** — all three resumed and delivered complete work on one re-prompt, including a finished 66-mutant batch that was never sent. Delivery is the failure, not the work.                                                                                                                                                                                                                                                                                                                                                                                  | session transcript `af80d1e7`: `Agent` `tool_use` entries counted by `subagent_type`, then the bare `teammate-message` envelopes compared against the `summary=`-bearing ones from the same rounds               | a silent lens that ALSO fails to answer a direct re-prompt — that moves this from "delivery is unreliable" to "the work is lost", a worse problem needing a different fix. Also any release changing subagent completion or notification delivery.    |
| F27 | 2026-08-01, Claude Code 2.1.220                  | What Claude Code can mechanically enforce, for anyone designing a gate. `UserPromptSubmit`, `UserPromptExpansion` and `SessionStart` are the only events whose **stdout is added as context the model can see and act on** — every other event's stdout goes to the debug log; `additionalContext` in `hookSpecificOutput` lands "alongside the submitted prompt". `PreToolUse` is the only hard gate (exit 2, or `permissionDecision: deny`), and it fires before the permission-mode check. `Stop`/`SubagentStop` can prevent a turn ending, but the hooks page documents **no loop guard**, so an unconditional Stop hook is itself a runaway. Skill invocation is always model-judgment — no hook or setting can force a skill to run, and `allowed-tools` is advisory. A subagent's verdict is a tool result, never binding. There is no harness-visible turn, token or iteration counter.                                                                                                                                                                                                                                                                  | `code.claude.com/docs/en/hooks` fetched directly, quoting the stdout-exception sentence and the Stop row; the no-loop-guard point is absence-of-documentation on that page, not a positive claim                 | any release changing hook events or their output contract — this table is the whole design space for enforcement, so a change here invalidates gate designs downstream                                                                                |
| F28 | 2026-08-01                                       | Two transferable primitives from the most-adopted open skills collection. **Completion Criterion**: a criterion must be _checkable_ ("Running `gh issue list --label needs-triage` returns issues that have been through initial classification", not "triage should work correctly") and _exhaustive_ ("every modified model accounted for", not "produce a change list"). **Negation** is a named failure mode: telling an agent what NOT to do makes the forbidden behaviour more available, because "the negation is a weak modifier that the strongly-activated concept can overrun" — the fix is to prompt the positive (`never write verbose comments` → `write one-line comments`), with prohibition reserved for hard guardrails and always paired with a positive. Our own rails are prohibition-dense (`CLAUDE.md` Tenet 04 is titled "What the hooks deny") and the failures observed this session were precisely the prohibited behaviours — claimed enforcement that did not exist, logged findings to look thorough, overrode a documented cap. That correlation is not a proven cause; the mechanism is named upstream and our evidence fits it. | `mattpocock/skills` `productivity/writing-great-skills` GLOSSARY and SKILL, via deepwiki, quoting both definitions                                                                                               | re-read if a positive-form rewrite lands and the same failure classes still recur — that would refute the mechanism for our case                                                                                                                      |
| F29 | 2026-08-01                                       | The most-adopted open skills collection has **no pattern for reporting diminishing returns or pushing back when the operator asks for more work than the task needs** — its only control is natural-language steering from the user. `spec-kit`'s `/speckit.converge` is the closest analogue and it measures against a spec rather than against effort spent. So the convergence signal is not a solved problem to copy; it is the piece to build.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | deepwiki over `mattpocock/skills`, asked directly and answered in the negative; `github/spec-kit` workflow phases                                                                                                | a release of either project adding a diminishing-returns or operator-pushback pattern — adopt theirs over ours if so                                                                                                                                  |
| F30 | 2026-08-02                                       | Rewriting a prohibition as a positive loses meaning in exactly one shape: the old text named a **specific** flag, artifact or output string, and the new text gives a general reason without closing the choice. Three instances in the positive-rails wave — `--no-verify` dropped from §7 entirely (the operator's harness denies it; the plugin ships nothing that does, so for a consumer the prose was the only thing there), "Do NOT use Workflows or /loop" softened into what they give up, and five phase-marker comment forms collapsed into a phrase that was also inaccurate. Every other rewrite held, several got stronger by prescribing the exact output. No contract criterion detects this class; all three were found by reading the diff sentence by sentence against the original.                                                                                                                                                                                                                                                                                                                                                          | this wave: `42961c1`, `26094d3`; the surviving repairs in `plugins/dossier/skills/build/SKILL.md` §7 and §Autonomous mode                                                                                        | any wave that rewrites rails — the sentence-by-sentence reading pass is the deliverable there, not the count                                                                                                                                          |
| F31 | 2026-08-16, Claude Code 2.1.233                  | A plugin's `bin/` directory is added to the Bash tool's `PATH` while that plugin is enabled, which is the supported way one plugin reaches another: `${CLAUDE_PLUGIN_ROOT}` names only the running plugin's own root. whetstone ships `bin/tiger-check` and `bin/claim-check`; dossier calls them by name, so a composed route reaches a consuming project by construction rather than by path derivation. The rejected alternative — deriving the sibling's cache path from the shared marketplace layout — is D4. **The documented scope is the Bash tool.** The reference states that positive and states no exclusion, so "therefore not hooks, MCP or LSP" is inference from silence, not a documented negative — the F27 evidence shape. The inference is acted on because the cost is asymmetric: resolving a full path works whether or not the PATH would have carried it. Re-read 2026-08-16 against Claude Code 2.1.233 after a rig wave designed a Stop hook around a bare-name `claim-check` call and would have shipped a gate that never resolved anything, with its own fail-open hiding the failure. A hook reaching a plugin executable reads `~/.claude/plugins/installed_plugins.json` for the install path; `${CLAUDE_PLUGIN_ROOT}` is exported only for hooks a plugin itself registered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `code.claude.com/docs/en/plugins`; `plugins/whetstone/bin/`                                                                                                                                                      | any release changing plugin `PATH` injection; a composed route reporting `skipped-absent` in a project where the sibling is enabled                                                                                                                   |
| F32 | 2026-08-16, corpus 3899 transcripts | Measuring agent behaviour from Claude Code transcripts has two traps, and each returns a plausible wrong answer. **Verdict strings are literal text in our own files** — `REVIEW: PASS` sits in `plugins/dossier/skills/build/SKILL.md` and in the `ARCHITECTURE.md` grammar table, so every read of those files scores as a verdict; a naive text scan reported 3036 verdict lines against a joined count of 71. **Subagents run asynchronously** — the `Agent` tool result is `Async agent launched successfully`, launch metadata only, so reading a verdict there found 6 across 96 `dossier-reviewer` spawns and concluded the agent was silent. The real return value arrives later inside a `<task-notification>` in a `user` record, keyed by `<tool-use-id>`. Joining on that id is the only correct route. | `tools/census/census.py`, whose module docstring restates both traps beside the code that avoids them | Re-derive, never cite: the corpus grows every session. Run `python3 tools/census/census.py verdicts` before any count here justifies a change. Recheck the traps themselves when Claude Code changes its transcript record shape or its subagent return path. |
| F33 | 2026-08-16, corpus 3899 transcripts, 313 notifications | Code-backed gates converge; one model-judgment gate has an always-finds-something bias. `dossier-reviewer` returned an on-grammar verdict on 57 of 59 notifications and paid a mean 0.43 failing passes before each success. `whetstone-doubter` returned `DOUBT: FAILURES` on **all 14** of its joined spawns and `NO FAILURE FOUND` on none of them. Separately and with no join, a literal string search across **all 313** notifications of every agent type found 2 `NO FAILURE FOUND` hits, both traced to text quoting the grammar rather than to a doubter verdict — the two populations are 14 and 313 and must not be read as one. One session paid 7 consecutive `DOUBT: FAILURES`. This is bias, not a proven runaway: the doubt-pass loop also ends at its cap and at nothing-new, neither of which this measurement observes. It is the measured argument for O21. | `tools/census/census.py verdicts` | Re-derive, never cite. Recheck when the doubter's prompt or its verdict grammar changes, and before O21 is scoped. |
| F34 | 2026-08-16, corpus 3899 transcripts | A model comparison this corpus cannot support, recorded so it is not re-derived. Operator-pushback rate read whole-corpus as `claude-opus-4-8` 5.7% against `claude-opus-5` 10.8%; restricted to the four days both models ran it reverses to 12.5% against 4.7%. The two overlap four days out of 47 and their effort settings barely intersect, so period and work confound the whole-corpus read and no model verdict follows from it. What holds in every cut: `opus-5` is **shorter** than `opus-4-8` — 32.2 against 35.6 messages and 2935 against 3734 characters per turn. `claude-fable-5` carries a self-correction in 0.0% of 304 turns, lowest of any model here. | `python3 tools/census/census.py turns`, whose pairwise section prints the restricted cut | Re-derive, never cite; the corpus grows every session and these figures moved between two runs a day apart. The pairwise cut is the load-bearing step — a whole-corpus number alone reproduces the error this row exists to prevent. |

## O — Open strides

Live rows only. A stride that lands is deleted, its mechanism promoted to a §F fact or its refusal to a §D row; git holds the rest.

| id  | stride                                                                                                                                                                                                                                               | why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | effort | state                                  |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------- |
| O1  | Rationale outlives the wave: decisions and their rejected alternatives survive a dossier's closure, surface at session start, and where mechanically detectable are caught at write time the way `invariant_guard.py` catches registered invariants. | The generalisation of D1. A closed wave's constraints go quiet on archive, so a later session re-proposes what was already rejected — at full cost, every time. This is the gap this file closes by hand and the plugin should close by itself.                                                                                                                                                                                                                                                                                                    | M      | open                                   |
| O5  | Cross-agent SKILL.md portability (Cursor, Codex, Gemini adapters).                                                                                                                                                                                   | Everything here is Claude-Code-locked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | L      | open — deliberately deferred           |
| O6  | Named, reusable persona registry, distinct from skills.                                                                                                                                                                                              | De-facto personas exist as one-off subagent prompts; the gap is a named reusable layer, not absence.                                                                                                                                                                                                                                                                                                                                                                                                                                               | M      | open                                   |
| O7  | Standalone ADR practice.                                                                                                                                                                                                                             | `§C` and `§I` are close but implicit; no ADR-authoring convention exists. Overlaps O1 — resolve them together or fold this in.                                                                                                                                                                                                                                                                                                                                                                                                                     | S      | open                                   |
| O8  | External distribution: directory listing, install and star visibility.                                                                                                                                                                               | Zero adoption signal. Own-marketplace suite UX landed; external presence did not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | S      | partial — `9656d89`, refined `fbc7c6c` |
| O13 | Widen `tiger-style` from one computed rule to several: per-language parsing so function length, assert adequacy and magic numbers move from the reviewer's advisory axis to computed checks.                                                         | The column budget landed as `code` — `tiger_check.py`, exit 0/1/2, wired at `ds:build` step 7. The other four TIGER rules ship as a manual-pass checklist plus a `Warn:`-capped reviewer axis: honest, and not enforcement. Moving any of them to `code` needs an AST per ecosystem, which was always the real objection — engineering, not premise. Measure the target codebase, never this repo's own source: this stride is about the code the plugins help write.                                                                              | L      | open                                   |
| O14 | Periodic harness spec and schema check — adopt new Claude Code capabilities and retire deprecated ones as a standing evolution rather than an occasional manual audit.                                                                               | Six hook events are wired in `hooks.json`; a renamed event makes a gate stop firing silently — no error, no failed test. `§F` records such facts by hand with a manual recheck column, which only works if a human remembers. `verify_authorities.py` is the prototype: registry, primary source, TTL cache, never blocks.                                                                                                                                                                                                                         | M      | open                                   |
| O21 | Wire two already-documented hook events the suite does not use: `PostToolUse` to lint a written SKILL.md, and `SubagentStop` to capture a subagent's verdict.                                                                                        | The harness documents 30 hook events; the suite wires 6. Both fix measured problems and both are consumer-facing, which raises their value above the first assessment: skill-smith's zero invocations were partly excused by this repo's CI lint, but consumers receive no CI, so outside this repo the gap is unmitigated. The doubter's verdict reaches the ledger only by prose instruction; a hook makes it deterministic. Enforcement delivered by a hook travels with the plugin; enforcement in CI does not.                                | M      | open                                   |
| O32 | Make `lint_skill.py` check trigger-phrase disjointness across skills instead of one `SKILL.md` at a time.                                                                                                                                            | The skill-authoring contract requires trigger phrases to be disjoint across skills, but the linter reads a single file per invocation and cannot see a collision. A colliding trigger passes the gate meant to catch it, and ambiguous routing stays invisible until one skill fires where another was meant. Found while authoring `tiger-style`, whose disjointness had to be checked by hand.                                                                                                                                                   | S      | open                                   |
| O31 | Count how often `--review` actually fires before widening anything that depends on it.                                                                                                                                                               | The tiger-style wave put four judgment rules on `dossier-reviewer`'s Standards axis, and that agent spawns only behind `--review` — opt-in, auto-firing solely for destructive-class tasks, a category orthogonal to code shape. D7 and F15 record what opt-in achieves here: the whetstone Skill wrappers saw near-zero direct invocations in 44 days. Widen the auto-fire once the census says what the rate is; guessing repeats the mistake D7 already corrected. Partly answered 2026-08-16: a repeatable census over `~/.claude/projects/**/*.jsonl` now exists, and F32 records the two traps that make a naive one wrong. What it cannot settle is the auto-fire rate itself — `dossier-reviewer` spawned 96 times against 13 `ds:build` invocations, because the operator's own Stop covenant spawns the same agent from outside this repo. Separating the two needs a marker the plugin sets, not a bigger census.                                                                              | S      | partial — census script exists, auto-fire rate still unreadable                                   |
| O30 | One `§S` parser shared by every consumer, with conformance fixtures, replacing the line grammar each enforcer transcribes for itself.                                                                                                                | Three landed fixes were all `§S` parser divergence between two enforcers, every one found by review rather than by the suite. B2 in the tiger-style wave was a fourth instance in a different set — the PAUSE classes, transcribed three times, with `review` missing from one copy. `test_pause_class_parity.sh` pins that one set, which treats the symptom. JSONL-`§S` is the recorded successor format: adopt it only if divergence recurs after the parser is unified, since the line grammar is hand-editable and that is most of its value. | L      | open                                   |
| O29 | Run the shape checks over this repo's own tree in CI, not only over the diffs `ds:build` produces.                                                                                                                                                   | `tiger_check.py` reads a staged diff, so this repo's pre-existing long lines and assert-free helpers are invisible to it by design. A CI pass over the whole tree would surface them. The caveat that has stalled it: F12 records that hook crashes fail dark, so adding asserts to hook code to satisfy an assert-density rule converts soft bugs into silently disarmed gates. Viable only if the tree check stays advisory and the hooks stay fail-open.                                                                                        | M      | open                                   |
| O36 | Carry the positive-form rewrite through the other 19 skill bodies and the two remaining agent bodies.                                                                                                                                                | The positive-rails wave took `CLAUDE.md`, `build/SKILL.md` and `dossier-reviewer.md` — the three with the most execution pressure — and left roughly 106 occurrences across the rest. The technique is settled (D14), so this is volume, not design. Do it per file with the same ceiling-plus-survivors contract; a single wave over 21 files would churn every table prettier touches.                                                                                                                                                           | M      | open                                   |
| O38 | Adopt [ponytail.dev](https://ponytail.dev)'s decision ladder — skip it → reuse an existing pattern → stdlib → native platform → an already-installed dependency → a one-liner → minimal new code — and site it where a skill body reads it.          | The survey and ranking this row used to ask for landed at 0a9036f, which moved the [`INSPIRATIONS.md`](./INSPIRATIONS.md) row from `never` to a classification; the ladder is the one item that row marks worth taking, and it is the ordered form of ceremony-net-negative. Adoption is the part still owed: today the ladder is prose in a lineage ledger, which no skill loads. Where it lands is the open question — a §D row, a rail in `ARCHITECTURE.md`, or a step inside `ds:new` — and picking wrong churns the same files twice.         | S      | open                                   |
| O39 | Build the Layer-2 live-model routing harness [`plugins/dossier/evals/README.md`](./plugins/dossier/evals/README.md) describes, and grow its case table past the five skills it seeds.                                                                | Layer 1 is a static trigger-phrase lint. It cannot tell whether `"where are we"` reaches `dossier:status` or `dossier:check`, which is the confusion the descriptions are written against, so the layer that would catch it has stayed a recipe with no artifact. It needs an API key, per-run cost and flake handling, which is why it stays out of the deterministic CI lane — a manual script plus a recorded baseline, not a CI job.                                                                                                           | M      | open                                   |
